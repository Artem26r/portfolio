export type ProfileGit = {
  repos: number,
  star: number
}

export type GithubError = {
    message: string,
    documentation_url: string
}

export type LocalBehance = {
  views: number,
  evaluations: number,
  subscribers: number
}